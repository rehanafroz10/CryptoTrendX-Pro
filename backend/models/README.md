# Models Directory

Trained ML model artifacts (`.pkl` / `.h5`) get saved here after running the
training pipeline (see `app/services/ml_model.py` -> `train_model()`).

This folder is gitignored for the actual model binary — only this README is tracked.
